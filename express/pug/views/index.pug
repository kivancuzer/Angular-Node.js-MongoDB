html
    head    
        title merhaba pug

    body
        //açıklama satırı
        //div(id = "baslik") burasi başlık 
        - var okul = "sakarya üniversitesi"
        - var sehir = "sakarya"

        =okul
        br
        =sehir

        hr
        | Bulunduğu şehir #{sehir.toUpperCase()}
        br
        hr

        - var sayi = 12;

        if !sayi
            | Sayı değişkeni yok.
        else if sayi === 12
            | Sayı 12.
        else 
            | Sayı 12 değil.
        hr
        =surname
        br
        span(style="color: white; background-color:black")
            | Merhaba #{name}
        br
        include footer.pug

        ul
            each val in["selam",2,3,4,5]
                li= val

        ul 
            each val, index in ["zero", "one", "two" ]
                li= index + ": " + val